// // import { createStore } from "redux";
// import { ADD_COURSE } from "./actions";

// const initialState = {
//   courses: [{ name: "Maths" }]
// };

// function CourseViewerApp(state = initialState, action) {
//   switch (action.type) {
//     case ADD_COURSE:
//       const courses = state.courses || [];
//       console.log(courses);
//       //   return [...courses, { name: action.name }];
//       return [...courses, { name: action.name }];

//     default:
//     //   return [...courses, { name: action.name }];
//   }
// }

// export default CourseViewerApp;
